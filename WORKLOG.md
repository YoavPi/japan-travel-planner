# Work Log

One line per delegated task, newest at the bottom. Maintained by the main
(manager) session after each sub-agent returns — sub-agents do not write here.

Format: `DATE | agent(s) | task | files | result`

---

2026-08-31 | (setup) | add architect / builder / growth / copywriter sub-agents + this log | .claude/agents/{architect,builder,growth,copywriter}.md, WORKLOG.md, CLAUDE.md | done
2026-08-31 | builder (role played in-session; agents not loaded from this cwd) | BUG: z260 map FABs (options/wrench, bank, day-reorder) float over the z120 "מצא ליד" NearbySearchSheet — root cause: `nearbyOrigin` missing from `overlayOpen` | src/views/EditorView.jsx:1354 | fixed; `npm run critical` ✓, eslint ✓, `npm run build` ✓ (main +2 B). Manual check still open: open a stop → "מצא מקומות באזור" → confirm FABs hidden while sheet is up (mobile viewport)
2026-08-31 | qa (role played in-session) | BUG: friend cannot open a map point's info popup — Windows + Chrome desktop only; works on his mobile + on Mac | investigated src/components/EditorMap.jsx (numbered-marker onClick), src/views/EditorDesktop.jsx (focusOnStop → cardCoord) | NOT root-caused from repo: click path is fully synchronous (no Places call gates the card), react-map-gl@7.1.7 / maplibre-gl@3.6.2 are current. Leading hypothesis: maplibre Marker synthetic-click suppressed as a micro-drag at OS/browser display scaling ≠ 100% (common on Windows). Needs diagnostics from the friend's browser — see summary.
2026-08-31 | builder ×9 (haiku/sonnet), qa ×9 reviews (sonnet/haiku), qa final review (opus), builder final fix wave (sonnet) — subagent-driven-development | Trip Files gallery: general files in trip.data.files[] (no schema/RLS change) + grouped TripFilesSheet (כללי→יום N) + AttachmentViewer + 🗂️ entry in mobile editor / desktop editor / both trip-overview pages; upload (with day picker) / inline-rename / move-between-groups / permanent Storage delete; demo-mode session blobs; day-renumber remap. Spec docs/superpowers/specs/2026-08-31-trip-files-gallery-design.md, plan docs/superpowers/plans/2026-08-31-trip-files-gallery.md | src/utils/tripFiles.js, src/services/attachmentService.js, src/hooks/useEditorState.js, src/components/{AttachmentViewer,TripFilesSheet}.jsx, src/views/{EditorView,EditorDesktop,TripOverviewView,TripOverviewDesktop}.jsx (+ 5 test files) | done — commits 7fcedc7..97de72c on saas-builder-local (11 commits, 2 task fix-rounds + 1 final-review fix wave); npm run critical 9/9, Jest 9 suites/50 tests, CI build no-warnings. Task 6 first commit (2396e71) accidentally swept pre-existing EditorView.jsx WIP → disentangled to wiring-only 6063528, WIP restored uncommitted. QA cases T-FILES-1..20 appended to docs/QA-TEST-PLAN.md (untracked). Follow-ups: pre-existing onAttachFilePicked ✅-toast bug; owner sanity-check on applyDateRange-shrink folding trailing-day files to כללי; manual RTL/dark/on-device pass.
2026-09-01 | deploy-sentinel (setup) | one-command prod deploy: `npm run deploy` = preflight -> `npx vercel deploy --prod --yes` -> verify:prod; deploy-sentinel now runs it automatically once the tree/branch gate is green (stops only on dirty tree / wrong branch); documented prod = saas-builder-local -> maslul-app.vercel.app and that main is frozen-by-design | package.json, CLAUDE.md (+ Deploy section), .claude/agents/deploy-sentinel.md | done. Open: prod currently ships via vercel --prod CLI (git push -> Preview only); optional cleanup = set Vercel Production Branch to saas-builder-local. First npm run deploy may need a one-time vercel login.
2026-09-02 | ai-engineer (B, in-session) + builder ×8 / ai-engineer ×2 (haiku/sonnet) + qa ×9 reviews + qa final review (opus) + builder final fix wave (sonnet) — subagent-driven-development | AI destination focus: when the AI trip's destination is a whole country/region, one step — curated FOCUS_REGIONS chips (10 countries × 3–4), a 1–3 city picker, or "תכנן לי" (skip) — becomes a hard (FOCUS) prompt constraint. B (shipped first, live): self-gating (GEO) rule + maxCities in api/_lib/prompt.js. Wizard gets the same chips as a city-step prefill (no AI there). Spec docs/superpowers/specs/2026-09-02-ai-focus-region-design.md, plan docs/superpowers/plans/2026-09-02-ai-focus-region.md | src/data/{destinations,focusRegions}.js, src/utils/destScope.js, src/components/{DestinationFocus,AiTripModal}.jsx, src/services/{googlePlaces,aiTrip}.js, api/_lib/prompt.js, api/generate-trip.js, src/views/WizardView.jsx (+ 7 test files) | done — B live (a06f3ad); A on saas-builder-local a06f3ad..f2eefeb (spec+plan+11 commits, 1 task fix-round + 1 final-review fix wave), NOT deployed. npm run critical 9/9, npm run test:api 17/17, Jest 14 suites/79 tests, CI build no-warnings. Final review (opus) BLOCKED on a shipped-class bug every English-only unit test missed: Places SDK loads language=he → predictions Hebrew → matchCuratedCountry English-only → curatedId always null → region chips never rendered in the modal; fixed in f2eefeb (Hebrew match + he aliases) along with FOCUS/GEO maxCities conflict, unvalidated focus→system-prompt, stale destScope on modal reopen, non-keyboard city rows, and a full FOCUS_REGIONS blurb/geo rewrite. QA cases T-FOCUS-01..15 appended to docs/QA-TEST-PLAN.md (uncommitted). Follow-ups: countryBias not wired to the in-phase city autocomplete (needs a country bbox); live-Gemini + on-device RTL manual pass.
2026-09-02 | (in-session: brainstorming + impeccable skills) | Product logo — icon-only route mark "מסלול": a planned route bending from a hollow start ring through an elbow to a destination pin, one colour #E0533F, no gradient. New BrandMark.jsx (inline SVG, currentColor, solid pin for 14–20px) replaces the ◈ placeholder in all 6 lockups (Legal/Footer/LandingView/LandingDesktop/Gallery/DashboardDesktop). Source SVGs + PDF + JPG in src/brand/; favicon.svg/.ico(16/32/48), logo192/512, apple-touch-icon, icon-maskable-512 regenerated from the SVG via headless Chrome + sips (no new deps). index.html: title→מסלול, theme-color #4f46e5→#E0533F, real description, SVG favicon link. manifest.json: names→מסלול, colours, maskable + apple-touch entries. | src/components/BrandMark.jsx, src/brand/*, public/{favicon.svg,favicon.ico,logo192.png,logo512.png,apple-touch-icon.png,icon-maskable-512.png,logo-mark.svg,index.html,manifest.json}, src/components/{LegalLayout,SiteFooter}.jsx, src/views/{LandingView,LandingDesktop,GalleryView,DashboardDesktop}.jsx | done — npm run critical 9/9, CI build compiled clean (+171 B), console clean, mark verified in mobile legal header + desktop nav + footer. Not touched: docTitle.js DEFAULT_TITLE still "Travel Planner" (in-app dynamic title, out of scope); SideMenu 52px circle left as user-avatar slot.

2026-09-03 | (in-session: superpowers:brainstorming) + new `budget-domain` agent | Trip budget — full design spec through collaborative brainstorm (13 clarifying rounds → 3 approaches → 3 design sections → spec). Scope: target budget + per-category caps, cost on any stop or as a standalone item, planned/paid/actual via one amount + "was it different?" prompt, dedicated `/trip/budget/:tripId` screen grouped by category, 4 entry points, 3 indicator surfaces + overrun alert, client-curtain sharing toggle, receipts attached to expenses surfacing as a "קבלות ותשלומים" group in the existing Files gallery, two-tier confirmation rule (`budgetImpact`). Persistence: existing JSONB, NO migration — recommended on the merits over a `trip_budgets` table (owner had lifted the DB restriction) because a transactional budget beside a blob-written itinerary splits the consistency model, would not fix concurrent editing, and a new RLS policy subquerying `trips` is this repo's documented recursion failure mode. v2 parked: AI estimation, cost splitting, multi-currency, per-item FX snapshot, real DB privacy. | docs/superpowers/specs/2026-09-03-trip-budget-design.md, .claude/agents/budget-domain.md, CLAUDE.md (agent table 9→10), WORKLOG.md | spec written, pending owner review; implementation plan not yet written. Two design bugs caught during the pass and folded in: (1) `budgetSummary` would go stale because stop-deletion writes through the generic `saveTrip(id,{data})` path, not `saveBudget` → recompute moved into `saveTrip` itself; (2) `duplicateStopAt` must strip the new stable `_id` or two stops share one expense. Named regression risk carried forward from the 2026-09-02 AI-focus incident: `guessCategory` takes HEBREW stop categories — its tests must use the verbatim strings from src/data/tripData.js, not English.

2026-09-05 | subagent-driven-development: builder-role implementers ×~14 (haiku/sonnet) + task reviewers ×10 (sonnet) + final whole-branch review (opus) + fix-wave + controller-completed steps after 4 consecutive subagent infra stalls | Trip budget Phase A — engine + dedicated screen, from docs/superpowers/plans/2026-09-03-trip-budget-phase-a.md (10 tasks). src/utils/budget.js: money in integer minor units, two-currencies-one-rate model, rollup() (planned/actual/effective, per-category caps, allocation) as the single numbers source, Hebrew-tested guessCategory, data transforms mirroring tripFiles.js, remapExpenseDays. tripService.saveTrip re-derives data.budget.summary on any budget-bearing patch (covers the generic editor save path too) and lifts it to trip.budgetSummary for the dashboard-list strip; no schema/RLS change. useBudget: optimistic state + rollback over the transforms. Money/BudgetSetupSheet/ExpenseSheet/ExpenseRow components; BudgetView at /trip/budget/:tripId (category grouping default, day toggle, progressbar with real agorot ARIA values, over-budget stated in words). useEditorState wired to remap standalone expenses' day on renumber; instanceId invariants (duplicate regenerates, update preserves) pinned by regression test. | src/utils/budget.js, src/services/tripService.js, src/hooks/{useBudget,useEditorState}.js, src/components/{Money,BudgetSetupSheet,ExpenseSheet,ExpenseRow}.jsx, src/views/BudgetView.jsx, src/App.jsx, package.json (jest.moduleNameMapper), src/setupTests.js (TextEncoder polyfill), docs/QA-TEST-PLAN.md (T-BUDGET-01..22), + matching *.test.js for every file above | done — commits bc3800e..43de352 (15 commits: 12 feature/fix + 2 plan-defect fixes + 1 test-infra shim) on worktree-trip-budget-phase-a (forked from saas-builder-local @ 73a4d7f). npm run critical 9/9, Jest 22 suites/223 tests, test:api 17/17, CI build clean. Two real plan defects found and fixed mid-implementation: (1) useBudget's re-sync effects keyed on trip.data object identity looped infinitely under an unmemoized caller — re-keyed on trip.lastEdited; (2) react-router-dom@7 is unresolvable under this repo's Jest 27 (broken package.json main + no `exports` support) — no prior test had ever imported the router; fixed with a moduleNameMapper + a TextEncoder polyfill, both additive. Final whole-branch review (opus) caught a shipped-class Critical the per-task reviews couldn't see (fixtures were JPY/ILS-only): ExpenseSheet inflated any 2-decimal foreign currency (EUR/USD/GBP/THB/AED/CHF) ×100 on a plain open-and-save; fixed + tested, plus 5 Important issues (currency-toggle silently re-denominating a paid amount; the trip-currency picker editable while foreign items exist; a zero target rendering a negative "remaining" + invalid progressbar ARIA; budget setup persisting all 8 base categories as empty cards; aria-modal="true" with no focus trap — dropped rather than left inaccurate, real trap deferred to the Phase D design pass already on the spec). Four consecutive subagent runs stalled on an infrastructure watchdog during the final fix wave and its re-review; the controller completed the fix (verified correct against each finding) and performed the scoped re-review directly rather than keep retrying — six rulings total, all logged with cost-if-wrong in the session. Follow-ups (explicitly out of Phase A): per-stop cost entry + StopActionsSheet, the editor quick-add chip, trip-overview/dashboard indicator surfaces + overrun toast, the ShareSheet budget toggle, the wizard setup step, receipts-on-expenses in the Files gallery, and a manual on-device RTL/dark-mode pass (T-BUDGET-21/22 need a real browser, not just jsdom).

2026-09-05 | (in-session, direct) + deploy-sentinel | Live-testing pass on the fresh Phase A deploy surfaced: no navigation to /trip/budget/:tripId anywhere in the app (Phase A shipped the screen, not its entry points — that's Phase C, called out explicitly beforehand but not loudly enough). Added a "₪" button beside the existing "🗂️ קבצים" entry in both EditorView (mobile) and EditorDesktop (desktop cockpit), routing to the budget screen; no live indicator yet. Also fixed one of two pre-existing, unrelated bugs the user hit while testing (confirmed via git history untouched by any commit this session): the desktop cockpit's anchored info-card Popup (hardcoded anchor="bottom" in EditorMap.jsx, disabling maplibre's built-in edge-flip) could render its photo/header above the visible browser window on a short window, with a tall bank-point-preview card variant exceeding the ~72%-viewport-height headroom the companion recenter effect provides — capped CARD_SHELL at ~68vh with internal scroll so the whole card is always reachable regardless of window height. Left open (not fixed): right-click context-menu clipping in EditorDesktop's openCtx — its edge-clamp uses guessed menu dimensions (230×260) instead of the real rendered size. | src/views/EditorView.jsx, src/views/EditorDesktop.jsx | done — commit 8fe83cb, npm run critical 9/9, Jest 223/223, CI build clean, deployed via deploy-sentinel to https://maslul-app.vercel.app (dpl_F84af2R2rXSTtDB37M4YYB38LEmF). Follow-up still open: the context-menu clamp bug.

2026-09-05 | (in-session, direct) + deploy-sentinel | Two more live-reported bugs fixed after further on-device testing: (1) budget screen's back button hardcoded to /trip/overview/:id — now navigate(-1), so it returns to wherever the user actually came from (the new editor entry point, or overview). (2) Places Inbox / points bank could permanently drop to showing 1 point instead of the full ~128 for the rest of a session: adding a point while the bank had never been opened flipped its `inboxPlaces===null` "never fetched" sentinel to a 1-item array via an optimistic-update shortcut, silently disabling both lazy-load fetch sites (inbox-mode open, 👁 eye toggle) for the remainder of the session. Fixed with a shared `mergeIntoInbox()` helper: keeps the instant optimistic feedback but also kicks off the real backend fetch and merges it in, instead of letting the shortcut permanently masquerade as "already loaded." | src/views/BudgetView.jsx, src/views/EditorView.jsx | done — commits 48331af, 3e395cd. npm run critical 9/9, Jest 223/223, CI build clean (no new warnings — mergeIntoInbox added to real dependency arrays, not suppressed). Deployed via deploy-sentinel to https://maslul-app.vercel.app. Not independently reproduced live (needs the user's real ~128-point bank data); root-caused from source with high confidence, asked the user to confirm on-device post-deploy. No test added — EditorView.jsx has no existing test harness to extend.

2026-09-05 | (in-session, direct) + deploy-sentinel | Right-click context-menu clipping fixed (the third live-testing bug this session): EditorDesktop's openCtx positioned the menu against a GUESSED size (innerWidth-230, innerHeight-260); a long place name or the day-submenu list could exceed that guess near a screen edge and clip. Now measures the real rendered box via a ref + useLayoutEffect and nudges it back on-screen before paint (re-runs when the day-submenu view swaps in). Desktop cockpit only. | src/views/EditorDesktop.jsx | done — commit 6d3c1dc, npm run critical 9/9, Jest 223/223, CI build clean. Deployed via deploy-sentinel to https://maslul-app.vercel.app. All three live-reported bugs from tonight's testing pass (info-card top-crop, points-bank 128->1, context-menu clipping) now shipped. Note: ~40 commits on saas-builder-local are live in production via `vercel --prod` but not yet pushed to origin on GitHub (by design per this repo's deploy flow — plain push only makes a Preview) — flagging since it's now a sizeable unpushed stack, push whenever backup/collaboration needs it.
2026-09-05 | (setup) | new `product-manager` sub-agent to own docs/ROADMAP.md; merged in a pasted Hebrew roadmap draft (הבא בתור/רעיונות/בוצע/חוב טכני) and corrected it against real ship history — budget item said "scope not closed" but Phase A is already deployed (2026-09-05), so moved it to בוצע and put Phases B/C/D under הבא בתור instead; also folded in Trip Files gallery (31/8, previously undocumented) and tonight's 3 live-bug fixes | .claude/agents/product-manager.md, docs/ROADMAP.md, CLAUDE.md (agent table + roadmap-maintenance rule), WORKLOG.md | done — CLAUDE.md now instructs invoking `product-manager` after any feature/fix ships or is scoped, in addition to (not instead of) this log
2026-09-05 | product-manager | add backlog idea: per-day lodging with automatic carry-over to the next day | docs/ROADMAP.md | done — item #16 under רעיונות, disambiguated from #1's AI-hotel-selection idea, open questions noted (day-level representation, mid-trip lodging change, interaction with useEditorState.js day-renumber/remap logic), pointed at architect+writing-plans for when it's scoped
2026-09-05 | product-manager ×2 | user requested full roadmap status + recommendation; agent found AI destination focus (2026-09-02) entirely undocumented in ROADMAP.md and fixed it | docs/ROADMAP.md | done — added Part B (self-gating GEO, commit a06f3ad) to בוצע and Part A (FOCUS_REGIONS chips, built+tested+reviewed at f2eefeb but not deployed) to הבא בתור as a near-zero-effort item; verified admin page (/admin, AdminRoute.jsx + isAdmin.js + api/admin/overview.js) already gates on yoav.pintel@gmail.com client+server-side, so no roadmap addition needed there
2026-09-05 | deploy-sentinel (gate check only; main session ran the deploy directly after the sub-agent correctly refused to trust a relayed user-approval it couldn't verify itself) | Deploy PM recommendation #1: ship AI focus Part A (already built/tested/reviewed at f2eefeb, sitting unreleased since 2026-09-02) | (no code changes — deploy only) | done — commit 709e2ee (includes 2 pending docs commits + everything already on saas-builder-local) deployed via `npm run deploy`; preflight/build/vercel upload/alias all green, verify:prod passed (Supabase + OAuth present) on https://maslul-app.vercel.app (dpl_74rtAUnboSe93rEM9gow8YSMjH6k). Gate had one wrinkle: untracked promo/STORYBOARD.md (pre-existing, unrelated) blocked deploy-sentinel's strict clean-tree check; user explicitly approved proceeding via AskUserQuestion, but the sub-agent declined to act on a relayed approval it couldn't verify itself — correct behavior — so the main session ran the gate + `npm run deploy` directly instead. Known non-blocking follow-up still open: countryBias prop on DestinationFocus.jsx not wired by its AiTripModal.jsx caller, so the focus-step city autocomplete isn't country-biased yet.
2026-09-05 | architect | Design pass for trip-budget Phases B/C/D (PM recommendation #2), reading the full spec (docs/superpowers/specs/2026-09-03-trip-budget-design.md) + live Phase A code | (design only, no code) | done — recommended split: B=itinerary binding (per-stop cost, 3 real defects found in shipped code), C=visibility/sharing, D=receipts, E=hardening/QA. Full Phase B design produced (files, 3 design decisions incl. one that needs owner sign-off — does the editor's ₪ chip switch from navigate-to-screen to quick-add-sheet — plus a spec refinement on confirm-vs-toast for undoable deletes), test plan, risk table. Surfaced 3 real findings in ALREADY-SHIPPED Phase A code, not just Phase B design: (1) useBudget's single-writer assumption would silently drop itinerary edits if mounted in the editor — must route editor-side budget writes through the editor's own commitData/persistTripData, not useBudget; (2) 3 mobile stop-clone paths (copyStopToDay, setStopAsMultiDayHotel, copyStopToOtherTrip in EditorView.jsx) don't regenerate instanceId, unlike the desktop path Phase A tested — becomes a real bug once stopRef exists in B; (3) LIVE BUG in production right now: EditorView.jsx's applyDateRange-shrink and reorderDays remap trip.data.files[] on day-change but not budget.items[] (useEditorState.js does remap both, desktop-only) — any day-tagged expense entered via the budget screen today can silently mis-tag or orphan its day after a mobile reorder/date-shrink. Not yet reported to the user's own decision on sequencing. | Pending: user decision on (a) whether to ship a B1-B4 "safety" bugfix slice standalone before the rest of Phase B, given finding 3 is a live prod bug, and (b) the ₪-chip UX change in decision 2 above.
2026-09-05 | budget-domain + builder + qa | Trip budget Phase B pre-work: standalone bugfix slice shipped ahead of the rest of Phase B, fixing a live prod bug found by architect's design pass | src/utils/{budget,budget.test,classify,classify.test}.js, src/views/EditorView.jsx, docs/QA-TEST-PLAN.md | done — commit 280e9fd deployed via npm run deploy, verify:prod passed on https://maslul-app.vercel.app (dpl_9FKdHRh3NRbL94vJmsJQpBFELmmo). Fixed: (1) mobile applyDateRange/reorderDays now remap budget.items[] day tags alongside files[] (previously files-only on mobile; desktop already did both) — a day-tagged standalone expense could silently mis-tag/orphan after a mobile date-shrink or reorder; (2) 3 mobile stop-clone paths (copyStopToDay, setStopAsMultiDayHotel, copyStopToOtherTrip) now regenerate instanceId via new withFreshInstanceId helper instead of carrying it forward. Also landed inert groundwork for the next phase (expensesForStop, detachStopExpenses, budgetImpact in budget.js — not yet called from any UI). npm run critical 9/9, 23 suites/246 tests, CI build clean. qa verdict "ship with follow-up": code-reviewed the one load-bearing property (shared `mapping` variable feeds both remaps, no drift risk) but could not exercise the actual drag-reorder/date-shrink gestures live (no dev server/browser tooling in that agent's session) — added T-BUDGET-23/24/25 to docs/QA-TEST-PLAN.md for a manual on-device pass still owed.
2026-09-05 | (in-session, direct) | Fixed the long-standing "🐛 באג קבצים על נקודה" (open since 2026-08-25, listed as an unfixed follow-up in the 2026-08-31 Trip Files gallery work): onAttachFilePicked addressed its target stop by array index captured when the native file picker opened; since the picker is async, a reorder/delete/day-switch while it was open could attach the file to the wrong stop (or none), while still showing a false "✓ צורף" success toast | src/views/EditorView.jsx, docs/QA-TEST-PLAN.md (T-FILES-21..24) | done — requestAttach now stamps+captures a stable instanceId (lazily, if the stop doesn't have one yet) plus the day number instead of a raw index; onAttachFilePicked resolves the target by (day, instanceId) at completion time and shows an honest "התחנה כבר לא קיימת — הקובץ לא צורף" toast if the stop is genuinely gone instead of a false positive. npm run critical 9/9, 23 suites/246 tests (no test added for this file — EditorView.jsx has no Jest harness, same documented gap as prior EditorView fixes this session), CI build clean (+120 B). Manual on-device verification of T-FILES-21/22/23/24 still owed (reorder-during-picker, day-switch-during-picker, deleted-target, legacy-no-instanceId stop).
2026-09-05 | (in-session, direct) | Deployed the files-attach bugfix (commit 1f730f7) | (no code changes — deploy only) | done — verify:prod passed on https://maslul-app.vercel.app (dpl_EmqMhcW29fQgZa94DTCe2eGVv1R9). All 3 PM-recommended items from this session now shipped: AI focus Part A, budget B1-B4 bugfix slice, files-attach race fix.
2026-09-05 | (in-session, direct) | Live-testing bug report: 3 symptoms in the Places Bank ("בנק הנקודות") — (1) "הבנק לטיול זה" tab shows ALL saved points across every trip, not just this trip's; (2) tapping a saved point's info card feels stuck, can't "minimize" without fully exiting the bank; (3) closing the bank zooms the map back out, losing whatever point/zoom the user was focused on. Root-caused (2) and (3) to ONE shared cause: closing inboxMode force-reset showAllSaved (the 👁️ eye toggle) to false, which made EditorMap's savedSig-watching fitBounds effect think the bank closed and snap the camera back to just the active day's route, discarding the user's zoom/focus with no way back short of reopening the bank | src/views/EditorView.jsx | done — removed the forced setShowAllSaved(false) from the inboxMode-close effect; the eye stays whatever the user (or the bank's own auto-on-open) last set it to, so the map no longer snaps away on bank close. npm run critical 9/9, CI build clean. (2) is not independently confirmed fixed — hypothesis is that losing zoom/overlay on close was what made the card feel "stuck"; flagged to the user to re-test after this ships. (1) is a SEPARATE, deeper issue: googleSavedPlaces.js's places_inbox table has NO trip_id column at all — listInboxPlaces() returns the same global set regardless of which trip's editor called it, so "הבנק לטיול זה" (bank for this trip) is mislabeled and behaves identically to a global bank today. Real fix needs a schema migration (trip_id column + backfill decision for existing rows + RLS/query update) — db-architect scope, not shipped in this pass. Not yet deployed — pending user confirmation this addresses (2)/(3) before shipping, and a decision on (1)'s scope.
2026-09-05 | db-architect + builder | places_inbox trip-scoping — the real fix for bug #1 from tonight's live-testing report ("הבנק לטיול זה" showing every trip's points, not just the current trip's) | db/migrations/supabase_migration_places_inbox_trip_id.sql (new), db/migrations/README.md, src/services/{googleSavedPlaces,tripService}.js, src/services/googleSavedPlaces.test.js (new), src/hooks/useEditorState.js, src/views/EditorView.jsx, docs/QA-TEST-PLAN.md (T-BANK-01/02) | done — db-architect added a nullable trip_id column to places_inbox (idempotent, no RLS change needed — ownership stays owner_id-only), threaded an optional tripId through listInboxPlaces/addInboxPlaces with a pre-migration fallback so deploying the code before running the migration is safe (behaves exactly as before until the column exists). Tagging decisions per call site (file:line in the agent's own report): requestInboxAt (mobile swipe-to-bank) and moveStopToInbox (desktop context-menu) now tag the current trip; explicitly-global flows ("🗂️ שמור בבנק הנקודות הכללי", custom-pin-to-bank, connectSavedPlaces) deliberately left untagged. builder wired the READ side: EditorView.jsx's inbox-mode fetch now calls listInboxPlaces(tripId) instead of unfiltered, confirmed the separate globalPoints effect correctly stays unfiltered. Found and documented (not fixed, out of scope): EditorDesktop.jsx's own "trip" bank tab uses a DIFFERENT, pre-existing mechanism — a client-side geographic bounding box, not trip_id at all — so desktop and mobile now use two different, inconsistent trip-scoping strategies; worth a follow-up to unify. npm run critical 9/9, 24 suites/252 tests, CI build clean. NOT YET APPLIED TO PRODUCTION DB — the migration SQL must be run manually in the Supabase SQL Editor before the trip_id tagging takes effect (code is backward-compatible and safe to deploy either before or after). Manual QA still needed: T-BANK-01/02 (needs live multi-trip data with saved points, not available in any agent session).
2026-09-05 | (in-session, direct) | Deployed the Places Bank fixes (commit 39f4956) | (no code changes — deploy only) | done — verify:prod passed on https://maslul-app.vercel.app (dpl_GiTheGCtzxLrHRkPnEE8pNQdaWKC). Eye-toggle-reset fix (zoom-out on bank close) is live and testable immediately. The trip-scoping fix (bank #1) needs the Supabase migration run manually before it takes effect — code is backward-compatible until then.
2026-09-05 | (user, direct) | Ran db/migrations/supabase_migration_places_inbox_trip_id.sql manually in the Supabase SQL Editor, per the instructions from the prior entry | places_inbox table (production Supabase) | done — user confirmed "Success. No rows returned" (expected for this DDL). The places_inbox.trip_id column now exists in production; the trip-scoping code deployed in commit 39f4956 is now fully active (no longer falling back to unfiltered behavior). T-BANK-01/02 in docs/QA-TEST-PLAN.md can now be manually verified live.
2026-09-05 | (in-session: superpowers:writing-plans) | Trip budget Phase B implementation plan — the remaining per-stop cost UI (B5-B9 in architect's earlier breakdown), building on the B1-B4 pure-layer groundwork already shipped today | docs/superpowers/plans/2026-09-05-trip-budget-phase-b.md | done — 8 tasks: (1) useEditorState.js saveStopCost/removeStopCost/costForStop + detach-on-delete/inbox-move, tested; (2) EditorView.jsx mobile mirror + money-aware undo toasts; (3) ExpenseSheet.jsx stop-bound mode, tested; (4) StopActionsSheet.jsx cost row, tested; (5) EditorDesktop.jsx wiring — context menu, budgetImpact Tier-1 confirms, ₪ chip quick-add; (6) EditorView.jsx wiring — same for mobile, StopActionsSheet cost row; (7) BudgetSetupSheet.jsx rate-change confirm, tested; (8) QA-TEST-PLAN.md T-BUDGET-27..35. Self-review caught and fixed one real design gap before finalizing: deleteStop needed to take idx as a parameter (not read actionsIdx from closure) because Task 6's confirm dialog defers the actual delete to a later tick, by which time the sheet that captured actionsIdx has already closed — fixed at the source in Task 2 rather than patched later, and the same fix applied to costFor's state shape (day+idx, not a bare index, to survive an active-day switch while the sheet is open). Not yet executed — plan only.
2026-09-05 | (in-session, direct) | Live-testing bug report: 3 mobile map-editor issues from screenshots — (1) the ⋯ stop-actions sheet is clipped at the top with no scroll (grouped menu is taller than a phone viewport, panel is bottom-anchored with no max-height/overflow, so top rows render off-screen and the "sticky" ✕ header does nothing); (2) the stop detail card shows the personal note read-only and only when it already exists — no way to add/edit it no matter how the stop was opened; (3) closing a stop card zooms the map back out to the whole-day frame instead of holding the user's view — should only re-frame on a day-menu tap | src/components/StopActionsSheet.jsx, src/components/EditorMap.jsx, src/views/EditorView.jsx | done (not yet deployed) — (1) panel now maxHeight: calc(100dvh - safe-area - 8px) + overflowY:auto + overscroll containment, so it scrolls and the sticky header works; (2) EditorMap stop card note block is now an always-present tappable row (shows note or "הוספת הערה אישית…") that opens the parent NoteSheet via a new onEditStopNote prop, wired to the existing saveNoteAt cascade, editable trips only, read-only fallback otherwise; (3) the day-fit effect in EditorMap was keyed on the pts array identity (changes on any incidental re-render — card open/close, keyboard resize, sheet snap — the holdView shield only covered the window while a card was open, not the close transition); replaced with a stable signature guard (activeDay + the day's rounded coordinate list) held in a ref, only re-fits when that actually changes, and does NOT record the signature while holdView/nearbyActive so a day switched with a card open still re-frames on dismiss. New activeDay prop threaded to EditorMap. npm run critical 9/9, eslint clean on all 3 files, 2 editor test suites/10 tests pass, CI build clean (+533 B). NOT verified live — the authenticated mobile editor (/map/edit/:tripId, ProtectedRoute) can't be exercised without credentials + a real trip in this session; the public demo (/map) uses MapComponent, not EditorMap, so it exercises none of this. Manual on-device pass owed for all 3.
2026-09-05 | deploy-sentinel | Deployed the 3 mobile map-editor fixes (commit 2ab7ab1: scrollable ⋯ StopActionsSheet, editable note on the stop detail card, hold-map-view-on-card-close) | (no code changes — deploy only) | done — gate green (tracked tree clean, branch saas-builder-local, untracked promo/ is the known non-blocker), preflight 9/9 + CI build, vercel deploy --prod first attempt (dpl_5hqvU3DE6B9mAL1v5ykoGkLFcLrL), aliased to https://maslul-app.vercel.app, verify:prod passed (Supabase + real Google OAuth present). No DB migration implied. NOT pushed to origin/saas-builder-local — production is ahead of the remote by these + the prior unpushed stack. Manual on-device QA of all 3 fixes still owed.
2026-09-05 | (in-session, direct) | Follow-up to the map-editor fixes: user reported tapping a map PIN no longer flies/zooms to the stop (only itinerary-row taps ever did — pin taps were pan-only on mobile). Wanted: tap a point (pin or row) → fly+zoom to it; exit → view stays put | src/views/EditorView.jsx, src/components/EditorMap.jsx | done (not yet deployed) — EditorView onActiveStop (the map-pin path) now also setFlyToCoord({lat,lng}) so a pin tap goes through the SAME flyTo-zoom-15 mechanism as navigateToStop's row tap; EditorMap marker onClick drops its redundant pre-pan when the parent owns onActiveStop (kept for a bare consumer). Close path unchanged and still inert: flyToCoord holds its last object so its effect doesn't re-run, day-fit sig unchanged → map stays where the fly landed. Note: tapping EMPTY map to dismiss still recenters on the tap point (pre-existing deliberate behavior, separate from ✕/swipe). npm run critical 9/9, eslint clean, CI build clean (+14 B).
2026-09-05 | (in-session, direct) | Follow-up #2: user asked that tapping EMPTY map area to dismiss a stop card also not move the map (it recentered on the tap point — a pre-existing "tap to recenter" behavior) | src/components/EditorMap.jsx | done (not yet deployed) — removed the panToCoord(e.lngLat) call from the map's blank-area onClick; the tap still dismisses the card / clears overlay / clears active stop / collapses the inbox, it just no longer pans. panToCoord still used by the bare-consumer marker fallback. npm run critical 9/9, eslint clean, CI build clean (-13 B).
2026-09-05 | (in-session, direct) | Follow-up #3: the editor day strip (numeric day chips in the schedule-sheet header) can't be horizontally scrolled with a wheel-only mouse (no trackpad / tilt wheel). Added mouse grab-to-pan + a hover-revealed slim draggable scrollbar | src/views/EditorView.jsx, src/index.css | done (not yet deployed) — new .day-strip-scroll class: scrollbar hidden by default (touch unchanged), on (hover:hover)+(pointer:fine) a thin draggable scrollbar appears on hover + cursor:grab. JS: onPointerDown/Move/Up on the strip container, mouse-pointer only (touch keeps native momentum), 4px threshold before it counts as a drag, pointer-capture during drag, RTL-safe delta scroll (el.scrollLeft = startLeft - dx), and an onClickCapture that swallows the trailing click so a drag doesn't also switch the day. Disabled in dayEditMode (chip pointer handlers own the gesture there). npm run critical 9/9, eslint clean (JS), CI build clean (+327 B js / +170 B css). Not verified live — auth-gated editor.

2026-09-06 | subagent-driven-development: builder-role implementers ×~10 (haiku/sonnet) + task reviewers ×8 (sonnet/haiku) + final whole-branch review (opus) + one final fix wave (sonnet) | Trip budget Phase B — per-stop cost, from docs/superpowers/plans/2026-09-05-trip-budget-phase-b.md (8 tasks). Built on the B1-B4 pure-layer groundwork shipped 2026-09-05 (commit 280e9fd). useEditorState.js/EditorView.jsx: saveStopCost/removeStopCost/costForStop (atomic stamp-and-link, single commitData/persistTripData write) + detach-on-delete/inbox-move across all 5 delete/move paths (2 desktop, 3 mobile). ExpenseSheet.jsx stop-bound mode (no day picker, label/category pre-seed). StopActionsSheet.jsx cost row. Both editors wired: context-menu/3-dot cost entry, Tier-1 budgetImpact confirms before any non-undoable budget-affecting action, money-aware undo toasts + re-link on undo for the undoable paths, ₪ chip switched from navigate to in-place quick-add. BudgetSetupSheet.jsx rate-change confirm when paid non-ILS items exist. Spec docs/superpowers/specs/2026-09-03-trip-budget-design.md, plan docs/superpowers/plans/2026-09-05-trip-budget-phase-b.md. | src/hooks/useEditorState.js, src/views/{EditorView,EditorDesktop,BudgetView}.jsx, src/components/{ExpenseSheet,StopActionsSheet,BudgetSetupSheet}.jsx (+ matching *.test.js for every testable file), docs/QA-TEST-PLAN.md (T-BUDGET-27..35) | done — worked in an isolated worktree (worktree-trip-budget-phase-b, forked from saas-builder-local @ f838ed7), merged clean (no conflicts, commit 9dab302) despite saas-builder-local having advanced 6 commits in the meantime from other in-session work. npm run critical 9/9→10/10 post-merge, Jest 26→27 suites/270→274 tests post-merge, CI build clean. Tasks 3/5/7 each needed one fix round — all three real bugs traced to defects in the PLAN's own sample code (a missing edit-mode guard, a missing theme token making a submit button invisible, an Escape-key handler closing the wrong dialog), not implementer error; logged with rulings in the SDD ledger. Final whole-branch review (opus) caught 2 real cross-task defects no single task's diff could show: (1) desktop's deleteDay permanently orphaned stop-linked expenses because remapExpenseDays deliberately skips stopRef items — the plan's own Task 5 had incorrectly told the implementer this was already handled; (2) mobile's moveStopIndexToDay could silently destroy a priced stop via dedupeDayStops's merge-on-name-collision behavior, discarding its instanceId — fixed to match desktop's already-correct non-deduping behavior. Also fixed in the same wave: ₪ quick-add wasn't gated on `editable` (a read-only/shared-trip viewer could submit an expense that silently vanished on reload — a real regression against the previously-safe navigate-only chip), mobile's ₪ chip had a stale aria-label, and mobile's costFor sheet state wasn't cleared on day-switch (stale re-seeded form). One residual, adjudicated but NOT fixed per the "no second fix wave" rule, surfacing here: a sibling of the moveStopIndexToDay bug exists in a DIFFERENT function, EditorView.jsx's moveStopToDay (the ⋯ quick-actions "העברה ליום…" menu) — same orphaning mechanism, unpatched, flagged as a fast-follow-up. Not yet deployed. Not covered by any Jest test (EditorView.jsx/EditorDesktop.jsx have no harness): manual verification of T-BUDGET-27..35 still owed.
2026-09-05 | (in-session, direct) | Private share lists — collaborator emails become owner-only + global-admin. Root cause: the share list was a `collaborators` JSONB column on `trips`, and RLS SELECT authorizes the whole row, so every recipient could read every other recipient's email from the raw payload (client-side hiding is theatre — visible in devtools). Fix: move the list into the existing per-recipient `trip_shares` table (owner sees all rows via owner_id; a recipient sees only their own row; a global admin sees all). New migration adds an `admin_emails` allow-list + `is_global_admin()` SECURITY DEFINER fn, back-fills existing JSONB collaborators into `trip_shares`, consolidates the 3 overlapping `trips` shared-read policies into ONE recursion-safe policy (`user_has_trip_share() OR is_global_admin()`), and an `edit`-share UPDATE policy. `trips.collaborators` column KEPT (deprecated, unused) for rollback. Client: tripService gains listShares/addShare/updateShareRole/removeShare (Supabase + localStorage branches); fetchAllTrips + fetchTripById resolve shared status/role from `trip_shares` not the JSONB; rowToTrip drops `collaborators`, tripPatchToRow won't write it; SharePermissionsModal rewired to the granular methods. MapCard/EditorView collaborator-avatar clusters degrade to hidden (owner sees the list in the Share sheet); ShareSheet.jsx is dead code, untouched. | db/migrations/supabase_migration_shares_private.sql (new), src/services/tripService.js, src/components/SharePermissionsModal.jsx, scripts/critical-checks.js, src/services/tripService.sharing.test.js (new) | code done — npm run critical 10/10 (was 9; +1 SHARING check), Jest 25 suites/256 tests, CI build clean, dev server compiles with no console/build errors. NOT deployed. Blocked on the owner: (1) run the migration in Supabase SQL editor BEFORE deploying, (2) smoke-test — share the 31-day map with an alternate email you control, confirm that account sees it under "שותפו איתי", opens it read-only, can ⭐ favourite and pull a point; then confirm a second shared account's devtools `trips` response has NO other emails. Follow-ups: a later migration drops `trips.collaborators`; optional `sharedCount` on the dashboard summary if the card's "+N" cluster is wanted back; no app UI yet for a global admin to view another owner's list (RLS permits it — query via Supabase console or a future admin panel).

2026-09-06 | (in-session, direct) | Fixed the residual sibling bug flagged in Phase B's final review: EditorView.jsx's moveStopToDay (⋯ quick-actions "העברה ליום…" menu) had the identical dedupeDayStops name-collision-merge bug already fixed in moveStopIndexToDay — a same-named stop on the target day would silently absorb the moved stop, discarding its instanceId and orphaning any linked expense | src/views/EditorView.jsx | done — commit follows this entry. Removed the dedupeDayStops call, matching moveStopIndexToDay's already-shipped fix and desktop's non-deduping moveStopToDay. npm run critical 10/10, Jest 27 suites/274 tests, CI build clean. Deploying trip-budget Phase A+B next.

2026-09-06 | (in-session, direct) | Deployed trip-budget Phase A+B to production (commits through df2784d, includes the moveStopToDay fix) | (no code changes — deploy only) | done — npm run deploy: preflight/build/vercel upload/alias all green. verify:prod FAILED on first run right after the alias switch (both Supabase-wired and Google-OAuth checks reported missing) — re-ran node scripts/verify-prod.js ~1 min later and both passed cleanly; confirmed a CDN/edge propagation delay, not a real regression (env vars unchanged in Vercel, 53 days old; local+remote build logs showed nothing abnormal). Live on https://maslul-app.vercel.app (dpl_6ze8GK5fkEtm337tKZd3DwrtNZL2). Deployed from a tree with another session's uncommitted, migration-pending work stashed out first (git stash push -u) so it couldn't leak into the production bundle; restored immediately after via stash pop, verified byte-identical to its pre-stash state.
2026-09-06 | (in-session, direct) | Follow-up to "Private share lists": migration run by the owner in the Supabase SQL Editor (confirmed "Success. No rows returned"), then committed + deployed | (no further code changes — commit + deploy of the above) | done — npm run critical 10/10, Jest 27 suites/274 tests, CI build clean, deployed and verify:prod passed. Smoke test still owed: share a trip with a second email, confirm that account sees it under "שותפו איתי" with no other collaborators' emails visible in devtools.
2026-09-06 | (in-session, direct) | Smoke test for the private share lists fix (d57fd91) — owner confirmed passed | (no code changes — manual verification only) | done — shared a trip with a second email, confirmed it appears under "שותפו איתי" and opens correctly, confirmed via devtools that the second account's API response contains no other collaborators' emails. Fix fully verified end-to-end.
2026-09-06 | (in-session: impeccable skill) + ux-critique | עיצוב מחדש — שלב תכנון ומוקאפים. נוצרו 4 סוכני עיצוב חדשים (`ux-critique`, `product-designer`, `landing-designer`, `design-systems`), CLAUDE.md 11→15 סוכנים. נכתבה תוכנית מלאה ב-5 שלבים עם 4 החלטות שנסגרו מול המשתמש (D1 בר עם תוויות · D2 מיזוג+העברה · D3 עיצוב מחדש אמיתי · D4 נחיתה בהירה בלבד). `ux-critique` אימת את E1–E13 והוסיף N1–N20; תיקן 4 מספרים שגויים בתוכנית שלי (EditorView.jsx 5,077 שורות ולא ~7,000; Icon.jsx 54 ולא 52; 2.34:1 ולא 2.35:1; E1/E4 היו לא מדויקים) — התיקונים הוחלו. נבנו 2 מוקאפים חיים ופורסמו כ-Artifacts: עמוד נחיתה (hero פוטוגרפי, CTA יחיד, מספרי יפן אמיתיים 31/154/10 מ-tripData.js) ועורך (בר תחתון לפני/אחרי + תפריט ⋯ 16→9 שורות). | .claude/agents/{ux-critique,product-designer,landing-designer,design-systems}.md, CLAUDE.md, docs/superpowers/plans/2026-09-06-redesign-landing-and-mobile-editor.md | תכנון בלבד — **אפס שינויי קוד מוצר**. ממתין לאישור המוקאפים ולהחלטה על N1 (צבע הכרטיס write-only) לפני Phase 1. באגים אמיתיים שנמצאו וטרם תוקנו: N2 (לינה נכשלת בשקט), N3 (העתקה בין-טיולית בלי משוב), N4 (הסרת קובץ מוחקת את האחרון בלי אישור), N6 (כל 9 צבעי הערים נכשלים בניגודיות), N7 (אין יציאה ממצב רציף), N8 (בנק לא נגיש בטעינה).
2026-09-06 | (in-session, direct) | תיקון N1 — "צבע הכרטיס" היה write-only. `_theme` נכתב משני בוררים (PASTELS ב-StopActionsSheet, THEMES בלחיצה ארוכה) אבל נקרא לרינדור רק ברשימת "מסלול רציף"; `renderPlaceRow` קיבע את הבאדג' ל-CHARCOAL/CORAL, כך שבציר הזמן הרגיל — המסך שהמשתמש מסתכל עליו — בחירת הצבע לא עשתה כלום, אי פעם. **לא** שוחזרו רקעי-כרטיס פסטליים: הערת Sprint 55 מתעדת שהם הוסרו במכוון, והמערכת השטוחה נשמרת. במקום זה הצבע נוחת על באדג' האינדקס — אותו אלמנט שתצוגת "רציף" כבר צובעת — כך ששתי התצוגות עקביות. נוסף `src/utils/contrast.js` (relativeLuminance/contrastRatio/readableInkOn) כי שתי הפלטות משתרעות מ-`#EBCB93` עד `#0D0F11` ו-`color:"#fff"` הקשיח נתן 1.55:1 על הבהיר ביותר — כולל ב-`:3939` שהיה הבאג היחיד הקיים בנתיב הרינדור הזה. קדימות: done > צבע המשתמש > לינה > ברירת מחדל. | src/utils/contrast.js (חדש), src/utils/contrast.test.js (חדש), src/views/EditorView.jsx | done — `npm run critical` 10/10, Jest 28 suites/284 tests (מהם 10 חדשים, כולל טסט שמוודא שכל 11 צבעי שתי הפלטות עוברים 4.5:1 עם ה-ink הנבחר), CI build נקי ללא אזהרות. פלטות מפוצלות (4 פסטלים מול 7 גוונים לאותו שדה) נשארו כפי שהן — זה N10, החלטת עיצוב ל-Phase 3. לא נפרס.
2026-09-06 | product-designer | Trip budget Phase C design spec — visibility/sharing layer (overview card, dashboard indicator, overrun toast, ShareSheet toggle, wizard step, receipts group) | docs/superpowers/specs/2026-09-06-trip-budget-phase-c-design.md (new) | done — spec only, no code. Designed all 6 surfaces with states/placement/interaction/copy against already-shipped Phase A/B code. Found 3 real issues predating this design: (1) src/components/ShareSheet.jsx is dead code (never imported) — the predecessor spec's §6 wrongly targeted it for the visibility toggle; live UI is SharePermissionsModal.jsx, spec redirects there and recommends deleting ShareSheet.jsx; (2) tripService.js's tripPatchToRow does a full-replace on `settings` (not a merge) — a naive `{settings:{budgetShared}}` patch would silently destroy destination/center/days/cityRanges/startDate on a live trip; spec flags this as the highest-risk line in the whole phase and requires spreading loaded trip.settings first, tracked as T-BUDGET-42; (3) the predecessor spec's phase table specifies the receipts group but no attach entry point — added one to ExpenseSheet. 3 open product decisions flagged for the owner, not resolved unilaterally: does budgetShared grant edit-collaborators write access to the budget or read-only (recommends write); should the flag default true for trips with existing collaborators (recommends universal false + one-time notice); is 85% the right near-cap threshold (flagged as a guess). Test IDs T-BUDGET-36..55 continue Phase B's numbering.
2026-09-06 | ux-critique | Heuristic UX audit of the mobile editor (EditorView.jsx), run before any redesign per project process | docs/superpowers/specs/2026-09-06-mobile-editor-ux-critique.md (new) | done — read-only, no code changes. Inventoried ~51 base-surface controls + 20-21 in StopActionsSheet across 35 ad-hoc z-index values. 25 ranked findings (6 P0, 13 P1, 6 P2). Most severe: two separate context menus (long-press vs 3-dot) for the same stop with 4 behavioral divergences incl. delete-with-undo vs delete-without-undo on the same action (F1); the whole secondary-action layer (bank, summary, dates, reorder) is unreachable on first load because the default sheet snap state can never be returned to (F2); moving a stop to the points bank has zero discoverable affordance anywhere in the product, only an untaught swipe gesture (F3); no exit from "continuous route" mode except the Home button (F4); every one of 13 city-color text/background combos fails WCAG contrast, 9 fail even the lower 3:1 bar (F5); EditorView.jsx has zero dark-mode usage and force-disables it on 3 theme-aware children (F6). Also found ~180 lines of dead/unreachable code (F7) and a FAB-suppression boolean missing 6 known overlays (F24). Handoff table routes findings to product-designer (IA/disclosure), design-systems (tokens/dark-mode/icons/z-index), builder (dead code + broken feedback/copy), qa (6 unverified device-dependent hypotheses). Deliberately does not propose solutions — recommends builder clear the dead code first, then product-designer decide with the owner whether this needs a redesign or targeted fixes.
2026-09-06 | landing-designer | Homepage redesign — MupDay-style hero/search/demo/phone-mockup, per docs/ROADMAP.md backlog idea #9 | docs/superpowers/specs/2026-09-06-homepage-redesign-design.md (new) | done — spec only, no code. Found real issues in the current LandingView.jsx/LandingDesktop.jsx before designing: 5 above-the-fold CTAs, 3 resolving to the same bare /auth URL with no memory of intent; ~89 lines of unreachable code (an AI waitlist modal that can never open, plus 2 metrics keys nothing reads); the active-trip hero branch is dormant on "/" (LandingGate redirects every authenticated user away — only reachable via the catch-all route); measured (not guessed) contrast failures on the #E0533F brand color in 3 different combos; PILLARS/T/INSPO/UNSPLASH/ArrowL all declared twice (mobile+desktop) with content drift; the marketing palette in src/index.css is entirely unused, landing pages hard-code 3 copies of the app palette instead. Designed 6 folds (hook+search, live demo, in-field screenshot, brand-color proof section, differentiation, ask) around a route-seam visual motif tying to BrandMark's own geometry. Search is keyless-by-design (local fuzzy match, no Places calls — explicitly citing this repo's own Places-cost incident history as the reason, not just the zero-keys demo rule). Live demo is a real MapLibre instance from bundled tripData, not an iframe. Phone mockup: desktop only, mobile gets the same capture unframed. All stats/evidence verified against real repo data (31 days/155 stops/10 cities/179 photos counted from source), explicitly left testimonials/usage-counts/logos empty rather than fabricate them. Flagged 7 decisions for the owner (biggest: whether a guest can complete wizard steps pre-auth) plus 2 performance findings (hero image 2.2MB, /public/photos/source 249MB across 179 files — needs a derivative pipeline before shipping).
2026-09-06 | builder | Mobile editor dead-code cleanup (F7+F24 from the ux-critique) — pure deletion/invariant-fix before any redesign | src/views/EditorView.jsx, docs/superpowers/specs/2026-09-06-mobile-editor-cleanup-report.md (new) | done — commit 3bb2f23, 5091->4882 lines (-209). Every one of the 7 dead-code claims (pendingStop modal, assignCard, liveOps&&!tripActive rollover, inboxGeoFilter, day-chip drag-reorder handlers, 3 unreachable ternaries, the continuousMode focus-modal branch) re-verified against live code before removal — all held up exactly as the critique described, nothing found reachable that shouldn't have been removed. overlayOpen boolean extended with the 6 missing overlay flags (filesSheetOpen, quickAddOpen, costFor, attachViewer, notePrompt, addChoice). npm run critical 10/10, 28 suites/284 tests, CI build clean. Pushed. Editor is now clear to redesign (F1/F2/F3 IA decisions) without designing around unreachable UI.
2026-09-07 | (in-session, direct) + impeccable skill | עמוד הבית השיווקי — נבנה מחדש לפי `docs/superpowers/specs/2026-09-06-homepage-redesign-design.md` (הספק של הסשן המקביל, כולל החלטות הבעלים Q1/Q2/Q6/Q7). `LandingView.jsx` + `LandingDesktop.jsx` → קומפוננטה רספונסיבית אחת; `LandingDesktop.jsx` נמחק (193 שורות של קבועים מוכפלים). שישה פולדים בקצב צילום←שחור-חם←נייר←הצפת-ורמיליון←נייר-2←שחור-חם. Hero: `hero_couple_fuji.png` האמיתי (Q7) עם שדה חיפוש שחוצה את קצה התמונה; פעולה יחידה — בחירת יעד. אין fold יעדים (Q6), אין AI ב-hero (Q1), אין eyebrows, אין רשת כרטיסים, אין משקל מעורב בכותרת. סריף `Noto Serif Hebrew` לתצוגה (כבר בייבוא ולא היה בשימוש). טוקנים שיווקיים כ-CSS custom properties ב-`LandingView.css` — מחליף 3 אובייקטי `T` קשיחים. חדשים: `utils/startTripLink.js` (רזולוור יחיד ל-deep-link, `SiteFooter` עבר לצרוך אותו במקום עותק inline), `utils/destinationSearch.js` (התאמה מקומית בלבד — לעולם לא Google Places), `components/DestinationSearch.jsx` (combobox ARIA מלא), `components/RouteSeam.jsx`, `scripts/build-landing-assets.mjs`. | src/views/{LandingView.jsx,LandingView.css}, src/views/LandingDesktop.jsx (נמחק), src/components/{DestinationSearch,RouteSeam}.jsx, src/components/SiteFooter.jsx, src/utils/{startTripLink,destinationSearch}.js + טסטים, scripts/build-landing-assets.mjs, public/photos/home/derived/ | done — `npm run critical` 10/10, Jest 30 חבילות/306 טסטים (+22 חדשים), CI build נקי ללא אזהרות. אומת חי בדפדפן על ה-build הפרודקשן. **שלושה באגים נתפסו במדידה ולא בעין:** (1) פיני המפה היו משוקפים מול קו המסלול שלהם — `insetInlineStart` נמדד מימין ב-RTL בעוד ציר ה-x של ה-SVG נמדד משמאל; היטל גאוגרפי לא מתהפך בעברית, תוקן ל-`left` עם נימוק בקוד (delta 0% אחרי). (2) ה-hero הביא שתי נגזרות לציור אחד כי היו גם `<source media>` וגם `srcSet` — פושט ל-`srcset+sizes` בלבד. (3) שורת הקופירייט בפוטר ב-`ink4` `#A4AAB1` = 2.34:1, הועבר ל-`ink3` = 4.93:1. סקרים ה-hero: ערכי §8.5 בספק נתנו 2.64:1 בפועל (הספק הניח שמיים, בפועל מעיל לבן ושלג ב-L≈0.999) — נפתר לאלפא מינימלי והודק ל-3.88:1 מדוד. **לא נפרס.** נותר: §8.4/Q5 בספק סותרים את D4, ה-fold של הגלריה (Q3) מסומן כמותנה ולא נבנה, ואין AVIF/WebP (אין encoder על המכונה).
2026-09-07 | ai-engineer + (in-session, direct) | תקלת ייצור בייצור מסלול AI — משתמש קיבל "יש כרגע עומס על שירות ה-AI" כשהיו לו 10/10 מסלולים פנויים. אותר בלוגי הריצה של Vercel: `2026-09-07T13:21:49Z POST /api/generate-trip → 502`, יעד "גאורגיה", 7 ימים, `stage:"generate"`. **לא מכסה** — הכשל הוא ב-Gemini: החזיר HTTP 200 עם תוכן שה-pipeline לא הצליח להפוך ל-`days` (שלוש אפשרויות שהקוד לא הבחין ביניהן: אין candidate כלל / finishReason SAFETY-RECITATION-OTHER / JSON בסכימה שגויה). הליקוי המרכזי: נתיב הכשל זרק את כל המידע האבחוני — `ai_errors` שמר רק משפט בעברית שנכתב למשתמש. תיקונים: (1) `callGemini` מחזיר `finishReason`/`blockReason`/`candidateCount`/`textLength`, ו-`callLLM` מצרף אותם + snippet של 200 תווים כ-`err.detail` שנכתב ל-`ai_errors.context.detail` — פלט מודל וסטטוס API בלבד, לעולם לא הטקסט החופשי של המשתמש; (2) `thinkingLevel` (ברירת מחדל "low", `GEMINI_THINKING_LEVEL`) + `maxOutputTokens` 12288→16384; (3) סינון `part.thought`; (4) `unwrapDays` לסכימות `{itinerary:[…]}`/`{trip:{days:[…]}}`; (5) retry גם על candidate ריק/חסום. UI: הודעת השגיאה הגנרית כבר לא טוענת לתקלה כלל-מערכתית ("עומס" נקרא כמו "הגעת למכסה" — בדיוק הקריאה השגויה שקרתה כאן), ונוסף `ErrorBox` עם כפתור "נסו שוב" שמשחזר את אותה בקשה (אותו focus/refine). | api/generate-trip.js, api/_lib/tripShape.js (חדש), api/_lib/tripShape.test.js (חדש), src/components/AiTripModal.jsx | done — `npm run test:api` 24/24 (7 חדשים), `npm run critical` 10/10, CI build נקי. **לא נפרס.** אומת בקוד ובלוגים בלבד — לא ניתן לקרוא את `ADMIN_DIGEST_TOKEN`/`GEMINI_API_KEY` (Vercel secrets) ולכן הסיבה המדויקת עדיין לא ידועה; האינסטרומנטציה נועדה בדיוק לכך במקרה הבא. סיכון שאותר וגודר: `thinkingLevel` מתועד ל-Interactions API ולא ל-`generateContent` — אם השדה לא מוכר התשובה היא 400 וכל ייצור נשבר, ולכן נוסף fallback חד-פעמי בסגנון `placesNewDisabled` הקיים בקובץ. ניתוח עלות: Places הוא ~97% מהעלות (28 קריאות × $0.032 = ~$0.90 למסלול 7 ימים) מול ~$0.02 ל-Gemini — המכסה השבועית הושארה על 10 בהחלטת הבעלים, כי הקיצוץ לא היה מונע את התקלה ולא מכוון לצוואר הבקבוק האמיתי.
2026-09-07 | (in-session, direct) | המשך לתקלת ה-AI — סולם שגיאות דו-שלבי לפי החלטת הבעלים: כשל ראשון → "יש עומס כרגע — אפשר לנסות שוב" + כפתור "נסו שוב"; כשל שני → "בניית מסלולים ב-AI לא זמינה כרגע. נסו שוב מאוחר יותר", הכפתור נמשך (ההודעה אומרת לחזור מאוחר יותר — להמשיך לבקש ניסיון סותר אותה). חיווי מפורש לתוצאת הניסיון השני: הצלחה → באנר `role="status"` "✓ הפעם זה עבד — הנה המסלול", כישלון → "גם הניסיון הנוסף לא הצליח" בראש ה-ErrorBox. שגיאות מערכת גולמיות לא מגיעות למסך בשום מסלול — כולל `accept()` שקודם הדליף `e?.message` מ-tripService/Supabase ישירות ל-UI. הודעות מכסה/cooldown/התחברות ממשיכות להיות מוצגות verbatim כי הן נכתבו למשתמש. | src/components/AiTripModal.jsx, src/components/AiTripModal.errors.test.js (חדש) | done — 4 טסטים חדשים שמקבעים את החוזה (כולל שתי טענות שמוודאות ש-"Gemini"/"500" לעולם לא מופיעים ב-DOM). Jest 31 חבילות/310 טסטים, `npm run test:api` 24/24, `npm run critical` 10/10, CI build נקי. הערה: "עומס" בכשל הראשון הוא ניסוח שהבעלים בחר במפורש; הוא תחום כעת לניסיון הראשון בלבד, והמסר השני כן כן ישיר.
2026-09-07 | (in-session, direct) | פריסה לפרודקשן של תיקון תקלת ה-AI (commit 1cf8812) — כולל בנייה מחדש של עמוד הבית (f65d36f) שהמתינה לא-פרוסה על הענף. `deploy-sentinel` נחסם ע"י ה-permission classifier, ולכן השער (ענף `saas-builder-local`, אין קבצים מנוטרים ששונו) נבדק ידנית ו-`npm run deploy` הורץ בהוראת הבעלים. | — | done — `dpl_AhmGf4wZRnwmyPmmKBSQbYtLdH1L`, alias ל-maslul-app.vercel.app, `verify:prod` עבר. `GET /api/generate-trip` מחזיר 401 auth-required תקין → הפונקציה עולה עם הקוד החדש. **לא אומת:** האם Gemini מקבל `thinkingLevel` ב-`generateContent` — נדרש ייצור אמיתי עם משתמש מחובר; שני המצבים בטוחים (יש fallback ל-400), ומופיעים אחרת בלוג: `[ai-generate] thinkingLevel rejected by the API` פעם אחת אם נדחה.
