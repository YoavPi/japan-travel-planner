# Roadmap — maslul-app

נשמר ע"י סוכן `product-manager` (`.claude/agents/product-manager.md`) — מתעדכן אחרי כל פיצ'ר/תיקון שיוצא, לא רק כשמבקשים עדכון. לא רשימת ספרינט מחייבת. כשרעיון עולה לעבודה בפועל, הוא עובר את הזרימה הרגילה (`architect` → spec → plan) ויכול לעבור מ"הבא בתור"/"רעיונות" ל"בוצע".

## 📋 הבא בתור

- **ניהול תקציב לטיול — שלבים C/D** — Phase A **בפרוד**. Phase B (B1-B9, הוצאה פר-עצירה) **בפרוד** (פורס 2026-09-06, כולל תיקון הבאג ב-`moveStopToDay`) — ראו "בוצע". לפי הספק ([docs/superpowers/specs/2026-09-03-trip-budget-design.md](../docs/superpowers/specs/2026-09-03-trip-budget-design.md), §10) נשאר: **Phase C — נראות/שיתוף/קבצים** (כרטיס בעמוד overview, אינדיקטור בדשבורד, טוסט+סימון חריגה, טוגל ב-ShareSheet, שלב באשף, קבצים מקושרי-הוצאה + קבוצת "קבלות ותשלומים" בגלריית הקבצים, הודעות Tier-2); **Phase D — הקשחה** (מעבר `ui-impeccable`, `copywriter` על מיקרוקופי/ניסוחי-אישור, `qa` מלא + T-BUDGET, critical/build) — "deploy-ready". המלצה (הצעה, לא החלטה): Phase A+B חי בפרוד עכשיו, אבל כל ה-QA הידנית התלויה (T-BUDGET-23/24/25, T-BUDGET-27..35, ראו "חוב טכני פתוח") עדיין לא בוצעה בפועל — כדאי לסגור אותה לפני פתיחת Phase C, כדי לא לערום עוד שכבת פיצ'ר על בסיס לא-מאומת.
- **החלפת שפה he/en** (בהשראת MupDay) — נדחה בכוונה; i18n מלא = חוב כבד (RTL + אלפי מחרוזות). לשקול קודם רק עמודי שיווק/משפט.

## 💡 רעיונות (Backlog)

1. הרחבות ל-AI trip-building — streaming, בחירת מלון/לינה, שדה תאריך התחלה (שעות פתיחה מדויקות), אופטימיזציית מסלול מתקדמת.
2. עוזר תכנון טיול (בוט על המסך) — שיחה חופשית + המלצות קונטקסטואליות סביב נקודה שנוספה.
3. טעינת מפות והעברת נקודות בין מפות — שיתוף/רכישה, ייבוא נקודות.
4. בחירה פרטנית בבנק (checkbox פר-נקודה) + סנכרון מועדפים ל-Supabase (כרגע localStorage בלבד).
5. מפות לרכישה / marketplace — קטלוג, entitlement, תשלומים (רחוק).
6. התראות אמיתיות — `NotificationsView` מוכן ומעוצב אך ריק; צריך מקור אמיתי (הזמנות שיתוף/עריכות/שמירות). קשור ל-#7 ו-#8.
7. התראות למייל / וואטסאפ — ערוץ התראות כללי לטיול, לא רק ל-#6/#8. מייל: כבר יש דרך Supabase; וואטסאפ: אינטגרציה חדשה (WhatsApp Business API / Twilio, כנראה דרך `vercel:marketplace`). לתכנן היטב לפני מימוש.
8. "עד מתי אפשר לבטל" על נקודה + התראה — שדה חדש על עצירה בציר הזמן (תאריך/שעה "ביטול חינם עד"), רלוונטי בעיקר להזמנות מלון/סיור/מסעדה; תלוי ב-#7.
9. עיצוב מחדש של עמוד הבית בסגנון MupDay — hero+חיפוש+דמו חי+phone mockup.
10. הפיכה ל-PWA/native app.
11. בניית טיול אוטומטית מכל הנקודות השמורות — "תבנה לי טיול עם כל הנקודות השמורות שלי": להזין את "כל הנקודות שלי" (הבנק הגלובלי, חוצה-טיולים) כקלט לצינור יצירת הטיולים ב-AI, במקום להתחיל מדף חלק. שאלות פתוחות: איך בוחרים אילו נקודות רלוונטיות (לפי יעד? הכל?), האם ה-AI רק מקבץ לימים או גם ממלא פערים, מה קורה במעט/הרבה מדי נקודות. שייך ל-`ai-engineer`.
12. חיבור API להזמנות (Booking וכו') — הזמנה/תמחור ישיר מהמסלול.
13. Gamification לפרסום מפות — למשל 10 שמירות על מפה של אחר → +10 ג'נרציות AI.
14. Onboarding מודרך בתוך העורך (contextual, שונה מ-5 המסכים הכלליים).
15. ייבוא הזמנה מ-Booking → מסלול מסודר לפי ימים אוטומטית.
16. הגדרת לינה ליום → המשך אוטומטי ליום הבא — לא #1 (שם מדובר על ה-AI *בוחר* מלון בזמן יצירה); כאן זו תכונת מבנה-מסלול לכל טיול, ידני או AI: המשתמש מציין את הלינה של יום מסוים, והיום הבא "יורש" אותה כברירת מחדל בלי להוסיף ידנית מחדש. סקופ לא פתוח — לפתח: איך "אותה לינה" מיוצג על יום (סוג-עצירה ייעודי? שדה ברמת-יום?), מה קורה כשמחליפים/מוחקים לינה באמצע טיול, איך זה מתנהג מול remap של ימים בשינוי-סדר/שכפול (יש כבר לוגיקת renumber דומה לקבצים ותקציב ב-`useEditorState.js` — יתכן שצריך מקבילה). לפני מימוש: `architect` + `superpowers:writing-plans`.
17. סקירה יזומה של שגיאות AI — `ai_errors` נכתב אבל אין תהליך סקירה שוטף; `api/ai-errors-digest.js` כבר קיים כ-endpoint מוכן-לתזמון אך לא מופעל/מתוזמן. המשתמש רואה רק הודעת שגיאה גנרית, בלי נראות לצוות על תקלות AI בפועל.

## ✅ בוצע

- **AI focus — שלבים A+B (FOCUS_REGIONS + self-gating GEO)** (B: 2026-09-02, בפרוד; A: 2026-09-05, בפרוד) — שלב B: כלל (GEO) + `maxCities` ב-`api/_lib/prompt.js`, יעד מדינה/אזור מוגבל למספר ערים לפי אורך הטיול בלי לפזר ימים בודדים בין ערים רחוקות; קומיט a06f3ad. שלב A: צ'יפי FOCUS_REGIONS + בוחר 1–3 ערים במודל ה-AI (`DestinationFocus.jsx`) — היה בנוי/נבדק/עבר ביקורת מ-2026-09-02 אך ישב בלי פריסה; פורס היום, קומיט 709e2ee, `verify:prod` עבר על maslul-app.vercel.app. המשך פתוח: `countryBias` לא מחובר — ראו "חוב טכני".
- **תקציב טיול — Phase A** (2026-09-05, בפרוד) — מנוע (`src/utils/budget.js`, יחידות-מינוריות שלמות, שני-מטבעות-שער-אחד, `rollup()`), מסך ייעודי `/trip/budget/:tripId`, נקודות כניסה בעורך (מובייל+דסקטופ). המשך בשלבים B/C/D — ראו "הבא בתור".
- **תקציב טיול — Phase B (B5-B9, הוצאה פר-עצירה)** (2026-09-06, **בפרוד**, מוזג ל-`saas-builder-local` בקומיט `9dab302`, פורס `dpl_6ze8GK5fkEtm337tKZd3DwrtNZL2`) — בנוי על גרעין B1-B4 (למטה). הוצאה פר-עצירה בשני העורכים (`saveStopCost`/`removeStopCost`/`costForStop` ב-`useEditorState.js`, כתיבה אטומית אחת) עם ניתוק-אוטומטי בכל 5 מסלולי מחיקה/העברה-לבנק (2 בדסקטופ, 3 במובייל); מצב מקושר-לעצירה ב-`ExpenseSheet`; שורת עלות ב-`StopActionsSheet`; אישורי Tier-1 (בלתי-הפיכים) לפני כל פעולה שמשפיעה על הוצאה מקושרת בשני העורכים; טוסטים money-aware לביטול-פעולה (undo) עם קישור-מחדש; צ'יפ ה-₪ בעורך עבר מניווט למסך תקציב להוספה-מהירה-במקום (ההתנהגות שתוכננה כאן קודם); אישור שינוי-שער-חליפין ב-`BudgetSetupSheet` כשיש כבר פריטים משולמים במטבע שאינו ₪. פותח בעבודת subagent-driven-development (worktree נפרד, מוזג נקי), נוספו 2 באגי-חתך שנתפסו רק בסקירת-ה-branch המלאה: `deleteDay` בדסקטופ יִיתם הוצאות מקושרות-עצירה (תוקן), ו`moveStopIndexToDay` במובייל יכול היה להשמיד עצירה מתומחרת דרך מיזוג-דה-דופ לפי שם (תוקן). `npm run critical` 10/10, 274 טסטים, build נקי. הבאג-האח שנשאר מהסקירה תוקן ב-2026-09-06 (קומיט `df2784d`) לפני הפריסה; QA ידנית עדיין פתוחה — ראו "חוב טכני פתוח".
- **תקציב טיול — Phase B1-B4 (bugfix + groundwork)** (2026-09-05, בפרוד, קומיט `280e9fd`) — שני באגים אמיתיים ב-Phase A שהתגלו ב-design pass של `architect` ל-Phase B: (1) `applyDateRange`/`reorderDays` במובייל לא remap-ו את `budget.items[]` בשינוי-טווח-תאריכים/סידור-מחדש, רק `files[]` (בדסקטופ `useEditorState.js` כבר עשה את שניהם) — הוצאה עצמאית מתויגת-יום יכלה להישאר עם `dayRef` תלוי-באוויר; (2) שלושה מסלולי שכפול-עצירה במובייל (`copyStopToDay`, `setStopAsMultiDayHotel`, `copyStopToOtherTrip`) שמרו את אותו `instanceId` — תוקן ע"י `withFreshInstanceId`. גם נחת גרעין אינרטי לשלב הבא (`expensesForStop`, `detachStopExpenses`, `budgetImpact` ב-`budget.js`, לא מחובר ל-UI עדיין). `npm run critical` 9/9, 246 טסטים, build נקי. **בדיקת QA ידנית על-מכשיר עדיין לא בוצעה בפועל** (T-BUDGET-23/24/25) — ראו "חוב טכני פתוח".
- **רשימות שיתוף פרטיות — תיקון דליפת אימיילים ב-RLS** (2026-09-06, בפרוד, קומיט `d57fd91`) — הבעיה: רשימת השיתוף ישבה כעמודת `collaborators` JSONB על `trips`, ו-RLS SELECT מאשר את כל השורה — כל נמען יכול היה לקרוא את האימייל של כל נמען אחר ישירות מה-payload (הסתרה בצד לקוח היא תיאטרון, גלוי ב-devtools). התיקון: מעבר לטבלת `trip_shares` פר-נמען + מדיניות RLS מאוחדת אחת (`user_has_trip_share() OR is_global_admin()`); המיגרציה הורצה בפרוד ע"י הבעלים לפני הדיפלוי. **מאומת** — סמוק-טסט ידני (2026-09-06): שיתוף עם אימייל שני, מופיע תחת "שותפו איתי" ונפתח כראוי, ותגובת ה-API באותו חשבון לא כוללת אימיילים של משתפים אחרים (נבדק ב-devtools). הבעלים אישר. סגור.
- **Trip Files gallery** (2026-08-31, בפרוד) — קובץ כללי לתחנה, גלריה מקובצת (כללי → יום N), אפלוד/עריכה/מחיקה מ-Storage. באג פתוח מהעבודה הזו — ראו "חוב טכני".
- בניית טיול ב-AI (Sprint 67–68) — פייפליין מלא Gemini+Places+haversine.
- גלריה ציבורית (2026-08-25) — פרסום/צפייה/מועדפים/gate להתחברות.
- תיקון באג התחברות Arc (2026-08-25) — bfcache + poll ל-session.
- הגנת עלות Google Places (2026-08-26) — quota + budget alert + finOps guard.
- ~~"מצא לי X באזור" מנקודה שמורה~~ — כבר בנוי בפועל (`NearbySearchSheet`), לא רעיון פתוח (התגלה תוך כדי תיקון באג כפתורים מרחפים).
- Sprint 69 — אנליטיקה, ציות, תיקון שיתוף/דמו.
- מעבר דומיין ל-maslul-app.vercel.app, סבב parity מובייל↔דסקטופ.
- **3 תיקוני UI מדווחים-חי בעורך** (2026-09-05, בפרוד) — חיתוך כרטיס-מידע עליון בדסקטופ, איפוס בנק הנקודות מ-~128 ל-1 אחרי הוספה, חיתוך תפריט קליק-ימני בקצה המסך; וגם כפתור חזרה בעורך תקציב שהיה מקובע ל-overview.
- **תיקון "באג קבצים על נקודה"** (2026-09-05, בפרוד, קומיט `1f730f7`) — פתוח מ-2026-08-25. הבעיה: `onAttachFilePicked` איתר את התחנה-יעד לפי אינדקס-מערך שנלכד ברגע פתיחת בורר-הקבצים הנייטיבי; מכיוון שהבורר אסינכרוני, reorder/מחיקה/מעבר-יום בזמן שהיה פתוח יכלו לצרף את הקובץ לתחנה הלא-נכונה (או לאף תחנה) — ותוך כדי כך עדיין הוצג טוסט "✓ צורף" מוטעה. התיקון: כתובת יציבה לפי (יום, instanceId) במקום אינדקס גולמי, וטוסט כנה אם התחנה כבר לא קיימת. `npm run critical` 9/9, build נקי, `verify:prod` עבר. QA ידנית על-מכשיר עדיין לא בוצעה — ראו "חוב טכני פתוח".
- **תיקוני "בנק הנקודות" מדווחים-חי** (2026-09-05, בפרוד, קומיט `39f4956`) — שני באגים נפרדים שדיווח המשתמש ישירות מבדיקה בנייד: (1) תוקן: סגירת הבנק אִיפסה בכפייה את טוגל 👁️ "הצג את כל הנקודות השמורות" ל-כבוי, מה שהחזיר את המפה ל-fitBounds של מסלול היום הפעיל בלבד ומחק את הזום/פוקוס של המשתמש — עכשיו העין נשארת איפה שהמשתמש השאיר אותה. (2) תוקן חלקית: טאב "הבנק לטיול זה" הציג את כל הנקודות השמורות מכל הטיולים, זהה ל"כל הנקודות שלי" — כי ל-`places_inbox` מעולם לא הייתה עמודת `trip_id`. נוספה עמודת `trip_id` (nullable, מיגרציה `db/migrations/supabase_migration_places_inbox_trip_id.sql` — **הורצה בפרוד 2026-09-05**) וקוד הקריאה/כתיבה תויג וסונן לפי טיול; תיוג-לפי-טיול בתוקף מלא כעת. טאב ה"טיול" בדסקטופ ממשיך להשתמש במנגנון שונה לגמרי (bounding-box גאוגרפי, לא `trip_id`) ולא נגע בו — ראו "חוב טכני" לאיחוד. `npm run critical` 9/9, 24 חבילות/252 טסטים, build נקי, `verify:prod` עבר.

- **3 תיקוני עורך-מפה במובייל מדווחים-חי** (2026-09-05, בפרוד, קומיט `2ab7ab1`) — התגלו ע"י המשתמש בבדיקה על-מכשיר, לא מפריט roadmap קיים. (1) `StopActionsSheet` (תפריט ⋯ פר-עצירה) נחתך בראש בלי גלילה — הפאנל עוגן-תחתון בלי `max-height`/overflow והתפריט המקובץ גבוה ממסך טלפון; עכשיו `maxHeight: calc(100dvh - safe-area)` + `overflowY:auto` + בלימת overscroll, והכותרת הדביקה עם ה-✕ עובדת. (2) כרטיס פרטי עצירה (מפין במפה או משורת מסלול) הציג הערה אישית קריאה-בלבד ורק אם כבר קיימת — עכשיו שורת-הערה קיימת תמיד (הערה או affordance "הוספת הערה אישית…") שפותחת את `NoteSheet` הקיים דרך prop חדש `onEditStopNote`, נשמר דרך אותו cascade של `saveNoteAt`, בטיולים ניתנים-לעריכה בלבד. (3) סגירת כרטיס עצירה החזירה את המפה למסגרת היום המלא במקום להחזיק את תצוגת המשתמש — אפקט ה-day-fit ב-`EditorMap` היה מפתח על זהות מערך `pts` (משתנה בכל re-render מקרי) ומגן ה-`holdView` כיסה רק את חלון פתיחת-הכרטיס; הוחלף בשומר חתימה יציב (`activeDay` + רשימת קואורדינטות מעוגלת של היום) ב-ref, ממסגר מחדש רק במעבר-יום אמיתי / שינוי סט-עצירות. `npm run critical` 9/9, 10/10 טסטי עורך, build נקי, `verify:prod` עבר. QA ידנית על-מכשיר לשלושתם עדיין לא בוצעה — ראו "חוב טכני פתוח". קבצים: `EditorMap.jsx`, `StopActionsSheet.jsx`, `EditorView.jsx`.

## חוב טכני פתוח

- אימייל ליצירת קשר — placeholder הוסר, לחבר כתובת אמיתית.
- `countryBias` ב-`DestinationFocus.jsx` לא מחובר ע"י `AiTripModal.jsx` — בוחר הערים הידני בשלב הפוקוס לא מוטה למדינה/אזור שנבחר (חסר bbox למדינה). קוסמטי/דיוק, לא באג נכונות. נמצא במהלך פריסת AI focus שלב A (2026-09-05).
- 🧪 QA ידנית עדיין לא בוצעה בפועל ל-T-BUDGET-23/24/25 (`docs/QA-TEST-PLAN.md`) — date-range shrink + day-reorder עם הוצאה מתויגת-יום, על מכשיר אמיתי (drag/date-picker לא ניתנים לאוטומציה בסביבת ה-QA הזמינה). קוד נבדק בעיון (`mapping` משותף לשני ה-remap-ים) אך לא הופעל חי. נפתח יחד עם Phase B1-B4 (2026-09-05, קומיט `280e9fd`).
- 🧪 QA ידנית עדיין לא בוצעה בפועל ל-T-FILES-21/22/23/24 (`docs/QA-TEST-PLAN.md`) — reorder-during-picker, day-switch-during-picker, deleted-target, legacy-no-instanceId stop, על מכשיר אמיתי (async file-picker race לא ניתן לאוטומציה בסביבת ה-QA הזמינה). נפתח יחד עם תיקון "באג קבצים על נקודה" (2026-09-05, קומיט `1f730f7`).
- דסקטופ ומובייל משתמשים כעת בשני מנגנוני trip-scoping שונים לטאב "הבנק לטיול זה": מובייל — עמודת `trip_id` אמיתית; דסקטופ (`EditorDesktop.jsx`) — heuristic של bounding-box גאוגרפי סביב עצירות הטיול הקיימות (`useEditorState.js`'s `loadInbox()` עדיין לא מסונן כלל). כדאי לאחד בעתיד. נמצא תוך כדי תיקוני הבנק (2026-09-05).
- 🧪 QA ידנית על-מכשיר עדיין לא בוצעה ל-3 תיקוני עורך-המפה במובייל (קומיט `2ab7ab1`, 2026-09-05) — גלילת `StopActionsSheet` + כותרת דביקה, שורת הוספת/עריכת הערה בכרטיס עצירה, והחזקת תצוגת המפה בסגירת כרטיס — על מכשיר אמיתי. העורך המאומת במובייל (`/map/edit/:tripId`, `ProtectedRoute`) לא ניתן להרצה בסשן סוכן; הדמו הציבורי משתמש ב-`MapComponent` ולא ב-`EditorMap`, כך שהוא לא מכסה כלום מזה. נפתח יחד עם הפריסה.
- 🧪 QA ידנית עדיין לא בוצעה בפועל ל-T-BANK-01/02 (`docs/QA-TEST-PLAN.md`) — trip-scoping אמיתי בבנק הנקודות מול נתוני multi-trip אמיתיים. עכשיו ניתן לבדוק חי בפועל (העמודה `trip_id` קיימת בפרוד מ-2026-09-05) — עדיין לא בוצע בפועל. נפתח יחד עם תיקוני הבנק (2026-09-05, קומיט `39f4956`).
- 🧪 QA ידנית עדיין לא בוצעה בפועל ל-T-BUDGET-27..35 (`docs/QA-TEST-PLAN.md`) — הוצאה פר-עצירה בשני העורכים, אישורי Tier-1, טוסטים money-aware, צ'יפ ה-₪ להוספה-מהירה, אישור שינוי-שער. `EditorView.jsx`/`EditorDesktop.jsx` בלי Jest harness, לא ניתן לאוטומציה בסביבת ה-QA הזמינה. נפתח יחד עם Phase B (2026-09-06, קומיט `9dab302`).

---

*מתוחזק ע"י סוכן `product-manager`. אחרי כל פיצ'ר/תיקון: מעביר ל"בוצע" את מה שבאמת נשלח לפרוד, פותח follow-ups תחת "חוב טכני" או "רעיונות" (בלי להמציא סקופ), ומציע — לא כופה — סדר עדיפויות חדש ל"הבא בתור" עם נימוק קצר.*
